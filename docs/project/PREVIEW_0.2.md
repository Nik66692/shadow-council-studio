# Preview 0.2

Windows internal preview containing the deterministic canon importer and Database Studio 0.2. The package is local-first, unsigned, bundles the approved Source of Truth v1.3, and must pass standard CI plus the Windows installer workflow before release.
