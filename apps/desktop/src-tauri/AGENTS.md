# Tauri/Rust instructions

Read `skills/database-migrations/SKILL.md` before changing migrations or repositories. Keep SQLite behind Rust services and map SQL errors to typed application errors before returning to the frontend.
