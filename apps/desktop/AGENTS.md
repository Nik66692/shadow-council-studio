# Desktop instructions

Read `skills/testing-and-ci/SKILL.md` before UI behavior changes. The frontend must call typed Tauri commands and must never access SQLite directly. Default UI copy is Italian.
